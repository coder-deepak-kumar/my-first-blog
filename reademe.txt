Needed Commands

1. create virtual environment in ubuntu 
    python3 -m venv myvenv

2. Start your virtual environment by running:
   source myvenv/bin/activate

3. Installing packages with requirements
    pip install -r requirements.txt